services:
  hello-py:
    # build: ./python

    image: harshit200525/python-app1:1.0

    ports:
      - 3000:3000
    environment:
      - ENV_VALUE=${ENV_VALUE}

  hello-node:
    # build: ./node

    image: harshit200525/node-app1:1.0

    ports:
      - 3001:3000
    environment:
      - ENV_VALUE=${ENV_VALUE}

  hello-springboot:
    # build: ./springboot/hello-spring
    
    image: harshit200525/sprintboot-app1:1.0

    ports:
      - 8081:8080
    env_file:
      - .env
      