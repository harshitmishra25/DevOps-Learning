services:
  hello-py:
    build: ./python
    ports:
      - 3000:3000
    environment:
      - ENV_VALUE=${ENV_VALUE}

  hello-node:
    build: ./node
    ports:
      - 3001:3000
    environment:
      - ENV_VALUE=${ENV_VALUE}

  hello-springboot:
    build: ./springboot/hello-spring
    ports:
      - 8081:8080
    environment:
      - ENV_VALUE=${ENV_VALUE_SPRINGBOOT}
      