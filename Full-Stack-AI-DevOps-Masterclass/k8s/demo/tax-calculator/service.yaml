apiVersion: v1
kind: Service
metadata:
  name: service-b
spec:
  type: ClusterIP
  selector:
    app: app-stack
  ports:
    - port: 4000
      targetPort: 4000  

---

apiVersion: v1
kind: Service
metadata:
  name: service-a
spec:
  type: NodePort
  selector:
    app: app-stack
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 32007